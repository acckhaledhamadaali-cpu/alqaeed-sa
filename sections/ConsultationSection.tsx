import { FormEvent, useState } from 'react';
import { MessageCircle, ShieldCheck } from 'lucide-react';
import SectionWrapper from '../components/SectionWrapper';
import Container from '../components/Container';
import SectionTitle from '../components/SectionTitle';
import SectionDescription from '../components/SectionDescription';

const WHATSAPP_NUMBER = '966511294383';
const SERVICES = ['????? ???????? ???? ???????', '?????? ????????', '????? ??????? ????????? ???????', '??????? ?????? ?????? ???????? ???????', '?????? ?????? ?? ????', '??? ??????? ?????? ??? ?????'];

export default function ConsultationSection() {
  const [businessName, setBusinessName] = useState('');
  const [businessType, setBusinessType] = useState('');
  const [service, setService] = useState('');
  const [branchCount, setBranchCount] = useState('');

  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const message = [
      '?????? ?????? ???? ?? ??? ??????? ?????? ??????? ??????? ???????.',
      `??? ???????: ${businessName || '?? ?????'}`,
      `??? ??????: ${businessType || '?? ?????'}`,
      `?????? ????????: ${service || '?? ?????'}`,
      `??? ??????: ${branchCount || '?? ?????'}`
    ].join('\n');
    window.open(`https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(message)}`, '_blank', 'noopener,noreferrer');
  };

  return (
    <SectionWrapper id="consultation-section" variant="white" spacing="dense">
      <Container id="consultation-container">
        <div className="max-w-3xl mx-auto rounded-2xl border border-border-subtle bg-surface-muted/30 p-5 sm:p-7 md:p-8">
          <div className="text-center mb-6">
            <SectionTitle id="consultation-title" level={2} className="font-bold">???? ??????? ????? ?????? ??????</SectionTitle>
            <SectionDescription className="mt-2 max-w-2xl mx-auto text-center">???? ???? ??????? ?????? ?? ??????? ?????? ????? ?? ??? ??? ?? ????? ??????.</SectionDescription>
          </div>
          <form onSubmit={handleSubmit} className="grid grid-cols-1 sm:grid-cols-2 gap-4" aria-label="??? ??????? ?????">
            <label className="flex flex-col gap-1.5 text-sm text-text-primary">??? ???????
              <input value={businessName} onChange={(event) => setBusinessName(event.target.value)} required autoComplete="organization" placeholder="????: ????? ????? ????????" className="w-full rounded-xl border border-border-subtle bg-white px-3 py-2.5 text-sm font-normal outline-none transition focus:border-secondary focus:ring-2 focus:ring-secondary/20" />
            </label>
            <label className="flex flex-col gap-1.5 text-sm text-text-primary">??? ??????
              <input value={businessType} onChange={(event) => setBusinessType(event.target.value)} required placeholder="?????? ?????? ?????? ????????" className="w-full rounded-xl border border-border-subtle bg-white px-3 py-2.5 text-sm font-normal outline-none transition focus:border-secondary focus:ring-2 focus:ring-secondary/20" />
            </label>
            <label className="flex flex-col gap-1.5 text-sm text-text-primary">?????? ????????
              <select value={service} onChange={(event) => setService(event.target.value)} required className="w-full rounded-xl border border-border-subtle bg-white px-3 py-2.5 text-sm font-normal outline-none transition focus:border-secondary focus:ring-2 focus:ring-secondary/20">
                <option value="" disabled>???? ??????</option>
                {SERVICES.map((item) => <option key={item} value={item}>{item}</option>)}
              </select>
            </label>
            <label className="flex flex-col gap-1.5 text-sm text-text-primary">??? ?????? <span className="text-text-muted font-normal">(???????)</span>
              <input value={branchCount} onChange={(event) => setBranchCount(event.target.value)} inputMode="numeric" placeholder="????: ??? ????" className="w-full rounded-xl border border-border-subtle bg-white px-3 py-2.5 text-sm font-normal outline-none transition focus:border-secondary focus:ring-2 focus:ring-secondary/20" />
            </label>
            <div className="sm:col-span-2 flex flex-col items-center gap-3 pt-2">
              <button type="submit" className="inline-flex items-center justify-center gap-x-2 rounded-xl bg-[#25D366] px-5 py-3 text-sm font-semibold text-white shadow-md transition hover:scale-[1.02] hover:bg-[#20ba5a] focus:outline-none focus:ring-2 focus:ring-[#25D366] focus:ring-offset-2"><MessageCircle className="w-5 h-5" aria-hidden="true" />????? ??? ????????? ??? ??????</button>
              <p className="flex items-center gap-1.5 text-center text-xs font-normal text-text-secondary"><ShieldCheck className="w-4 h-4 text-secondary" aria-hidden="true" />?? ?????? ??? ???????? ?? ??????? ????? ??? ??? ?????? ?? ??????.</p>
            </div>
          </form>
        </div>
      </Container>
    </SectionWrapper>
  );
}
